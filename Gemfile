
source 'http://rubygems.org'

# Base requirements

# sets the core version
dm_version = '1.0.2'

gem 'dm-core', dm_version 

## DM ADAPTERS

group :dm_sqlite_adapter do 
  gem 'dm-sqlite-adapter', dm_version 
end

group :dm_mysql_adapter do 
  gem 'dm-mysql-adapter', dm_version 
end

group :dm_yaml_adapter do 
  gem 'dm-yaml-adapter', dm_version 
end


## DB FUNCTIONALITY GROUPS

group :data_mapper do 
  gem 'data_mapper', dm_version 
end

group :dm_aggregates do 
  gem 'dm-aggregates', dm_version 
end

group :dm_constraints do 
  gem 'dm-constraints', dm_version 
end

group :dm_migrations do 
  gem 'dm-migrations', dm_version 
end

group :dm_serializer do 
  gem 'dm-serializer', dm_version 
end

group :dm_timestamps do 
  gem 'dm-timestamps', dm_version 
end

group :dm_types do 
  gem 'dm-types', dm_version 
end

group :dm_validations do 
  gem 'dm-validations', dm_version 
end

### OTHER DM GEMS

group :dm_adjust do 
  gem 'dm-adjust', dm_version 
end

group :dm_is_list do 
  gem 'dm-is-list', dm_version 
end

group :dm_is_tree do 
  gem 'dm-is-tree', dm_version 
end

group :dm_is_versioned do 
  gem 'dm-timestamps', dm_version 
  gem 'dm-is-versioned', dm_version 
end

group :dm_observer do 
  gem 'dm-observer', dm_version 
end

group :dm_tags do 
  gem 'dm-tags', dm_version 
end




## TESTING REQUIREMENTS
group :test do 
  # gem 'mocha'
  gem 'rspec', :require => "spec"
end



